# Public site env
# Point to your API base (dev defaults to localhost)
NEXT_PUBLIC_API_BASE=http://localhost:4000
# Branding & badge
NEXT_PUBLIC_BRAND=XRPL Demo
NEXT_PUBLIC_NETWORK_LABEL=devnet

# Optional: hide the Check page from the top nav (page still accessible directly)
NEXT_PUBLIC_HIDE_CHECK_NAV=false
